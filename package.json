{
  "name": "pr-review-daemon",
  "version": "1.0.0",
  "description": "Local daemon that monitors GitHub PRs and triggers Claude reviews",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot run src/index.ts"
  },
  "dependencies": {
    "marked": "17.0.1",
    "pino": "10.3.0",
    "pino-pretty": "13.1.3"
  },
  "devDependencies": {
    "@types/bun": "1.2.4"
  }
}

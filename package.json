{
  "name": "pr-review-daemon",
  "version": "1.0.0",
  "description": "Local daemon that monitors GitHub PRs and triggers Cursor reviews",
  "main": "server.js",
  "scripts": {
    "start": "node -r dotenv/config server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}

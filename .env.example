# PR Review Daemon Configuration
# Copy to .env and update values: cp .env.example .env

# Your GitHub username
GITHUB_USERNAME=

# Review mode:
# - ONLY_OWN_PRS=true  -> Only review your own PRs
# - REVIEW_OWN_PRS=true -> Review all PRs (yours and others)
# - Both false -> Only review others' PRs
ONLY_OWN_PRS=true
REVIEW_OWN_PRS=false

# Poll interval in seconds
POLL_INTERVAL=60

# Repos to monitor (comma-separated)
# Leave empty to monitor all PRs involving you
#
# Exact match: owner/repo
# Regex: /pattern/ (e.g. /mycompany\/.*/ for all repos in org)
#
# Examples:
#   REPOS=mycompany/repo1,mycompany/repo2
#   REPOS=/mycompany\/.*/
#   REPOS=mycompany/specific-repo,/mycompany\/service-.*/
REPOS=

# Directory for cloned repos and reviews (defaults to daemon directory)
# WORK_DIR=~/pr-reviews

# Web server port for browsing reviews
WEB_PORT=3456

# Sync mode: "auto" (poll automatically) or "manual" (trigger from UI)
SYNC_MODE=auto

# Number of PRs to review in parallel
PARALLEL_REVIEWS=3

# Cleanup settings
CLEANUP_INTERVAL_HOURS=24
CLEANUP_AGE_DAYS=7
